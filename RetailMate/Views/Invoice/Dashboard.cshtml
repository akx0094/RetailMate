@* @model IEnumerable<RetailMate.Models.Invoice>

@{
    ViewData["Title"] = "Invoices Dashboard";
}

<h2>All Invoices</h2>

<table class="table">
    <thead>
        <tr>
            <th>Bill No.</th>
            <th>Date</th>
            <th>Customer</th>
            <th>Total</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var invoice in Model)
        {
            <tr>
                <td>@invoice.BillNumber</td>
                <td>@invoice.Date.ToShortDateString()</td>
                <td>@invoice.CustomerName</td>
                <td>₹@invoice.Items?.Sum(i => i.Subtotal) ?? 0</td>
                <td><a asp-action="Details" asp-route-id="@invoice.Id" class="btn btn-sm btn-outline-primary">View</a></td>
            </tr>
        }
    </tbody>
</table>

<a class="btn btn-primary" asp-action="Create">New Invoice</a>
 *@